#include <stdio.h>
#include "main.h"
/**
 * _abs - returns absolute value of the number
 * @n: int parameter
 * Return: the absolute value
*/
int _abs(int n)
{
if (n < 0)
return (-1 * n);
else
return (n);
}
